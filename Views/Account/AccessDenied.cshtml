@{
    ViewData["Title"] = "Akses Ditolak";
}

<div class="text-center mt-5">
    <h1 class="text-danger">Akses Ditolak</h1>
    <p class="lead">Anda tidak memiliki akses ke halaman ini.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Kembali ke Home</a>
</div>
